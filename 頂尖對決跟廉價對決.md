# 頂尖對決跟廉價對決：TLPD 車牌 OCR 本機對照

> 2026-09-24 本機實驗紀錄。「頂尖／廉價」只是這場對照的玩笑標題，不代表模型售價、授權價值或獨立測試排名。這份文件可直接交給谷寶寶檢討，但**不**可把下列數字當作正式上線辨識率。

## 一句話判決

同一批 3,032 張 TLPD 車牌中心影像，作者公開的 CPM 校正＋LPRNet 在本機重播得到 **2,974/3,032（98.09%）**；我們現役 `active-v1` OCR 將影像直接縮放後得到 **67/3,032（2.21%）**。只取符合 v1 三英文字母＋四數字的 2,044 張，現役模型也只有 **66/2,044（3.23%）**。目前最大的實際問題不只「找不到牌」，還有 **OCR 無法辨認真實車牌**。

## 對照結果

| 路線 | 輸入與裁切 | 完整字串正確 | 同批 v1 牌型 | 備註 |
| --- | --- | ---: | ---: | --- |
| 作者公開模型 | 車牌中心圖 → CPM 四角 → 94×48 透視校正 → LPRNet greedy | 2,974/3,032（98.09%） | 2,010/2,044（98.34%） | 作者模型的**訓練來源 TLPD 重播**，不是獨立測試。 |
| 現役 `active-v1` | 車牌中心圖 → 直接縮放至 380×160 → 官方預處理／受規則約束的 CTC 解碼 | 67/3,032（2.21%） | 66/2,044（3.23%） | 目前 bundle 的訓練來源為合成資料；全批包含 988 張非 v1 格式。 |
| 現役 `active-v1`＋作者 CPM＋本專案 M3a | 預測四角 → 380×160 透視校正 → 同一 OCR | 6/3,032（0.20%） | 6/2,044（0.29%） | 108 張四角被 M3a 拒絕；兩套校正裁切規格不同，**不能**把此列解讀為純 OCR 比較。 |
| 現役 `active-v1`＋TLPD 標註四角＋M3a | 僅已取得標註且符合 v1 的 549 張 | 2/549（0.36%） | 2/549（0.36%） | 4 張四角越界；只是局部幾何診斷，不是完美標準裁切或正式測試集。 |

同一批 549 張的輔助核對：作者路線辨對 540 張；現役直接縮放辨對 13 張。現役直接縮放的原始 greedy 有 **1,609/3,032 張輸出空字串**；規則解碼不能替代模型學到真實字形。

## 速度：不要跨硬體喊贏

| 路線 | 本機執行環境 | 穩態 P50 / P95 | 範圍 |
| --- | --- | --- | --- |
| 作者 CPM＋LPRNet | RTX 5060 Ti／PyTorch CUDA | 7.377 / 10.209 ms | 預處理、CPM、透視、OCR、解碼；不含讀檔與載入模型；10 張暖機後、無背景下載的重跑。 |
| 現役直接縮放＋CTC | ONNX Runtime CPU | 76.858 / 91.109 ms | 含讀檔、縮放、預處理、ONNX、受規則約束解碼；10 張暖機後。 |

現役路線的 P50 分段：ONNX 約 **1.040 ms**，CTC 解碼約 **72.115 ms**。這是解碼效能問題，但先把解碼器跑快，**不會修好 3.23% 的 v1 真牌正確率**。兩路線的硬體、裁切、計時範圍不同，速度數字不是公平的排行榜。

## 責任切分：不要讓定位器替 OCR 背鍋

| 責任 | 必須交付與證明 | 不能用什麼搪塞 |
| --- | --- | --- |
| 車牌定位 | 全景照片中的正確整牌框／四角、漏檢與誤檢率、多車牌結果。 | OCR 讀不出已正確裁下的牌，不是定位器可以解決的事。 |
| 幾何校正 | 四角拓撲與邊界有效、輸出 380×160、字形與安全邊距不被切掉。 | 不能拿裁切失敗的分數直接宣稱 OCR 權重差；也不能把可讀裁切的 OCR 失敗推回定位。 |
| **谷寶寶：辨識模型／訓練** | 在已確認可讀的真實車牌 crop 上提高完整字串正確率；交付資料分割、訓練設定、權重來源、逐張預測、空輸出與錯字分析。v1 至少先單獨報告 `LLLDDDD`。 | 不能只報合成圖分數、挑好看的示例、用規則強湊字串，或拿同一資料訓練又驗證的數字宣稱泛化。 |
| 本專案評測／整合 | 鎖定資料版本與評測腳本、核對標籤與同號變體、固定可讀 crop，比較前後相同樣本；模型通過後才考慮 Reader/API 切換。 | 不能把作者的 98.09% 訓練來源重播當獨立驗收，也不能把不同硬體速度直接相除。 |

因此下一輪驗收要先分兩張成績單：**給定正確 crop 的 OCR**，以及**全景圖到最終字串的端到端流程**。前者由 OCR 負責，後者才是定位、校正、OCR 的共同結果。OCR 未單獨過關前，不要用「再換更強 Detector」解釋同一張可讀裁切圖的錯字。

## 公平補考的最低條件

1. 以車牌字串和近重複照片分組切 train/validation/test；同一車牌的變體不能跨組。測試集先凍結，調參時不看答案。
2. 先人工複核測試圖的車牌文字與 crop，特別標出不屬於 v1 `LLLDDDD` 的牌型。TLPD 檔名推得的文字目前只是**暫定標籤**，不能假裝已逐張審完。
3. 固定相同測試圖，分別報完整字串 exact match、空輸出率、各牌型樣本數、失敗清單；不得只報字符級命中率。附模型／資料／程式版本和訓練來源。
4. 用未參與新模型訓練與調參的真實照片做最終檢查，再測全景定位到 OCR 的端到端結果與同硬體延遲。未做到之前，任何「98%」都只限於所述實驗條件。
5. 新模型不得直接覆蓋 `active-v1`；先保存為候選 bundle，跑完回歸、授權和來源檢查後再決定是否啟用或發行。

## 證據與邊界

- TLPD 影像 revision：`00f9ae2fa3d186bcf74e7f6ef4f48280181d6e85`；3,032 張 JPG 均已對上游 hash。檔名推得 2,632 個不同車牌字串，僅抽查部分圖像；LabelMe JSON 沒有獨立 OCR 文字欄位。本機 JSON 只取得 1,363/3,032，**不是**完整成對下載。
- 作者程式：[`evan6007/FPGA-LPR`](https://github.com/evan6007/FPGA-LPR) commit `574667ca7f5730d17b4b6fcda3ec568521bcbcd8`；[公開權重](https://huggingface.co/evan6007/FPGA-LPR) revision `51b9606b174eedbc091aec844c288a72aa9cd25b`。這是車牌中心圖的 CPM＋OCR 示範，**不是**完整全景定位服務。
- 現役 `active-v1` `recognizer.onnx` SHA-256：`e9aef185f19b0b5293de41b2a22b701ed228773033bd0c1eb26a5439cd25e8ac`；未修改或啟用外部模型。
- 本機逐張結果與摘要：`runs/external-lpr-bakeoff/full-3032-clean.{jsonl,summary.json}`、`active-direct-3032.{jsonl,summary.json}`、`active-cpm-3032.{jsonl,summary.json}`、`active-gt-v1-available.{jsonl,summary.json}`。此目錄、資料集和權重均為本機忽略檔，**不進 Git／release**。
- 本次只有離線測試與文件；沒有重新訓練、換模型、啟用到 Web Studio／API，也沒有 browser、IIS 或 production 驗收。
